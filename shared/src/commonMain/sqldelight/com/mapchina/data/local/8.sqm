DROP TABLE IF EXISTS carving;
CREATE TABLE carving (
    id TEXT NOT NULL PRIMARY KEY,
    user_id TEXT NOT NULL,
    region_id TEXT NOT NULL,
    region_name TEXT NOT NULL,
    image_path TEXT,
    stroke_data TEXT,
    created_at INTEGER NOT NULL
);
